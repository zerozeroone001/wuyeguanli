<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysOpinionConsultationMapper">

    <insert id="insertOpinionConsultation" parameterType="SysOpinionConsultation" useGeneratedKeys="true" keyProperty="consultationId">
        insert into sys_opinion_consultation(
            user_id,
            title,
            content,
            source_type,
            source_id,
            status,
            create_by,
            create_time,
            update_by,
            update_time
        ) values(
            #{userId},
            #{title},
            #{content},
            #{sourceType},
            #{sourceId},
            '0',
            #{createBy},
            sysdate(),
            #{updateBy},
            sysdate()
        )
    </insert>

</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysMeetingOpinionMapper">

    <insert id="insertOpinion" parameterType="SysMeetingOpinion">
        insert into sys_meeting_opinion(topic_id, user_id, opinion, create_time)
        values(#{topicId}, #{userId}, #{opinion}, sysdate())
    </insert>

</mapper>
